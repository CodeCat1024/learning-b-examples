# 配置要连接到哪个数据库
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/usermanager?characterEncoding=utf8
    username: root
    password: 1111
    driver-class-name: com.mysql.cj.jdbc.Driver

# 开启日志打印
logging:
  level:
    com:
      zjt: debug

# 这个的含义是什么呢？
mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
