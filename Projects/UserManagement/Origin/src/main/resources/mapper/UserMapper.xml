<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--要设置实现接口的具体包加类名-->
<mapper namespace="com.demo.usermanager.mapper.UserMapper">
    <update id="update">
        update userinfo set username = #{username},
        <if test="password!=null">
            password = #{password},
        </if>
        sex = #{sex}, age = #{age}, address = #{address}, qq = #{qq}, email = #{email}
        where uid = #{uid}
    </update>

    <delete id="del">
        delete from userinfo where uid = #{uid}
    </delete>

    <delete id="dels">
        delete from userinfo where uid in
        <foreach collection="ids" item="uid" separator="," open="(" close=")">
            #{uid}
        </foreach>
    </delete>

    <select id="login" resultType="com.demo.usermanager.model.UserInfo">
        select * from userinfo where loginname = #{loginname} and password = #{password} and isadmin = 1
    </select>

    <select id="getAll" resultType="com.demo.usermanager.model.UserInfo">
        select * from userinfo;
    </select>

    <insert id="add">
        insert into userinfo(username, loginname, password, sex, age, address, qq, email, isadmin)
        values (#{username}, #{{loginname}, #{password}, #{sex}, #{age}, #{address}, #{qq}, #{email}, #{isadmin})
    </insert>

    <select id="getUserByLoginName" resultType="com.demo.usermanager.model.UserInfo">
        select * from userinfo where loginname = #{loginname}
    </select>

    <select id="getUserByUid" resultType="com.demo.usermanager.model.UserInfo">
        select * from userinfo where uid = #{uid}
    </select>

</mapper>